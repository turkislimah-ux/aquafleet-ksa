# SUPERSEDED — do not read this file for state

**This file is a pre-Arabic-effort orphan.** Its contents were a snapshot that
stopped being maintained long before Phase 3 (the English → bilingual Arabic
conversion) began, and every figure in it has since moved. Acting on it would be
acting on stale state.

**The current handoff is `.planning/HANDOFF.md`.** That is the one file
`CLAUDE.md` §5 names as ours, and it is the only handoff that is kept current.

Read order for a fresh session:

1. `CLAUDE.md` — the standing rulebook; §7 is the durable money/schema record.
2. `.planning/HANDOFF.md` — current state.
3. Recent `git log`.

**Nothing is lost.** The prior contents of this file are in git history:

```
git log --follow -- HANDOFF.md
git show 078bf0e:HANDOFF.md      # the last maintained version
```

This file is kept tracked, rather than deleted, so that anyone who opens it by
name lands here instead of on nine-month-stale state.
