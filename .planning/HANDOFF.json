{
  "version": "1.0",
  "timestamp": "2026-08-15T13:30:00.000Z",
  "source": "claude-code-session",
  "partial": false,
  "phase": null,
  "phase_name": null,
  "phase_dir": null,
  "plan": null,
  "task": null,
  "total_tasks": null,
  "status": "water-station-cost-complete-verified",
  "completed_tasks": [
    "WATER STATION COST - a deliberate P&L change, migrations 0110-0114, through commit 98dbfea. CLAUDE.md section 7 carries the full entry.",
    "0110 per-water-type station pricing (fill_cost_potable_sar / fill_cost_non_potable_sar) + trips.filling_cost_sar, the frozen per-trip snapshot - commit 76f0957",
    "Capture phase: station pricing form, trip-add type gating, server-side snapshot at creation - commit 35c3798",
    "0111 backfilled 703 historical trips (6,150.00) and validated the at-least-one-type CHECK - commit 119814b",
    "0112 filling enters the money model as a DAILY-SOURCED DIRECT COST - five operating-cost buckets, and direct_cost_sar carries it - commit 5499c20",
    "0113 exposed filling_cost_sar + filling_uncosted_trips on v_pnl_by_period so quarter and year costs foot - commit 6f9a445",
    "Reports: cost statement gained a Station fill section (by type, by station, total, uncosted count) and the P&L gained its fifth bucket line - commit f574a4c",
    "The station-change GATE (stationBlockedForType / decideStationChange, both server actions, both pickers) - commit 7f462af",
    "Dashboard: Cost Mix gained its fifth slice and foots again; direct-cost line confirmed reading v_daily_operations.direct_cost_sar; disclosure names filling; uncosted count travels with the money - commit 4494b54",
    "Test 4 - tests/trip-station-gate.spec.ts, 8 tests, standing (no diagnostic route, no auth bypass) - commit 4564419",
    "0114 the DATABASE guarantee - trips_station_type_guard_ins/_upd, raising 23514 with the same sentence the app produces - commit 08d9580",
    "Move-trip modal now shows the real block reason instead of 'Could not move this trip. Try again.' (onApply returns { ok, error }) - commit 441123b",
    "createTrip's 'NULL is a real outcome' comment corrected - that path is unreachable for new trips since 0114 - commit 1a783f7",
    "Dead-code sweep of the feature: two unused exports made module-private, four stale comments corrected - commit 19080c0",
    "updateTrip DELETED - dead, and unsafe if it were not (wrote station/type with no gate and no re-snapshot) - commit da924a8",
    "One shared cost-bucket colour source (lib/cost-colors.ts) - the two doughnuts had Payroll and Outsourced SWAPPED against each other - commit 98dbfea",
    "CLAUDE.md section 7 records the whole feature, all fifteen commit hashes verified to resolve - commit 180a27e",
    "FIX c76e731 - 'becoming delivered' was read as closed history, so a station change riding along with a move to delivered kept the OLD station's price. Found by Turki on the Reports cost sub-tab; blast radius exactly one trip; KI-026-0062 corrected to 80.00 and the fix re-verified in-browser on a second trip (KI-026-0075, 5.00 -> 80.00, Shas row 2 fills / 160 SAR)."
  ],
  "remaining_tasks": [],
  "blockers": [],
  "human_actions_pending": [],
  "decisions": [
    "NULL IS NOT 0.00 AND THAT IS THE WHOLE MODEL. A price SET - including 0 - means the station OFFERS that water type; NULL means it does not. 0 is a real price (company-owned stations fill free), so it can never be collapsed into 'no price'. Any ?? 0, Number(x) || 0 or truthiness check reintroduces the bug the schema was shaped to prevent.",
    "THE FREEZE IS AGAINST PRICE EDITS, NOT AGAINST CHANGING WHICH STATION FILLED. Editing a station's price later must not reprice history, but a trip that actually filled elsewhere is a wrong record, not a protected one - so a station change RE-TAKES the cost. THE COST IS FROZEN ONLY WHEN THE TRIP IS CLOSED HISTORY, AND CLOSED MEANS DELIVERED AT BOTH ENDS OF THE WRITE. in_transit->delivered RE-TAKES (becoming delivered is still live - the fill has just been asserted to have happened somewhere else); delivered->delivered is frozen; delivered->in_transit RE-TAKES (reopened); loading->loading RE-TAKES. Reading it as 'the target stage is delivered' is the bug that shipped and reached live data (c76e731). Keying on the CURRENT stage alone would be wrong in the other direction.",
    "costPatch: null (do not touch) and { filling_cost_sar: null } (moved, new station does not price this type) are DIFFERENT CLAIMS and the type keeps them apart. Writing null over a closed trip's cost would wipe a reported figure. decideStationChange's third parameter is named closedHistory, NOT isDelivered - the wrong name is what made the wrong value look right at the call site, and renaming it was part of the fix rather than cosmetic.",
    "A SCHEDULED TRIP'S FROZEN COST IS NOT A COST YET. v_filling_cost_monthly joins stage in (loading, in_transit, delivered), so a scheduled trip contributes nothing. Summing trips.filling_cost_sar RAW therefore EXCEEDS the statement and looks like the view is understating - live, August carries 20 scheduled trips holding 165.00 that correctly do not appear. This produced a false alarm during verification: a predicted total was 5 SAR out purely because a scheduled trip's cost was subtracted from a figure it had never been part of. Reconcile against the view's own predicate, never a raw trip sum.",
    "THE GATE IS ONE RULE READ FROM TWO DIRECTIONS - selectableWaterTypes narrows the TYPE for a fixed station (trip creation), stationBlockedForType narrows the STATION for a fixed type (station change). Only the first existed at capture time, and KI-026-0062 proved it: a potable in-transit trip moved to Umm Al Hamam, which does not fill potable at all. Both live in lib/station-pricing.ts and the actions call ONE pure decideStationChange.",
    "0114 IS A TRIGGER, NOT A CONSTRAINT, because 13 potable trips at Umm Al Hamam predate per-type pricing and are legitimately grandfathered. A CHECK is retroactive and NOT VALID only defers it - a later VALIDATE, or a pg_restore which re-checks everything, would reject the dump. A BEFORE trigger inspects only the NEW row. It applies at EVERY stage including delivered: grandfathering means the existing rows stay, not that more may be made.",
    "TWO triggers, not one: OLD cannot be referenced in an INSERT trigger's WHEN clause, so a single INSERT OR UPDATE trigger would bury the IS DISTINCT FROM test in the body and every unrelated update would enter the function to be told to leave.",
    "pg_trigger_depth() > 1 on UPDATE, because trips_water_station_fkey is ON UPDATE CASCADE - renaming a station KEY rewrites water_station on every trip pointing at it, which looks exactly like a station change and would re-validate the grandfathered rows and refuse a legitimate admin rename. Same technique as 0096's 30-day lock.",
    "SECURITY DEFINER on the trigger function because the failure mode matters: if water_stations RLS is ever narrowed, an INVOKER lookup returns no row, 'not found' allows, and the guard FAILS OPEN silently exactly when someone tightened security. Enforcement must fail closed. No revoke footer - Postgres refuses to invoke a trigger function directly and EXECUTE is checked at CREATE TRIGGER time.",
    "The trigger WRITES NOTHING. filling_cost_sar stays the app's job because the freeze rule lives there; splitting the money rule across SQL and TypeScript is how the gate came apart the first time.",
    "filling_uncosted_trips IS HISTORICAL-ONLY since 0114 - new trips can no longer be uncosted, so the count is fixed at 13 (June-July). A NEW uncosted trip appearing means the guard was dropped, not that the data changed. The count still travels wherever the money is shown, because sum() skips NULLs and those 13 make the historical total short by an unknown amount.",
    "create or replace view can only APPEND (0112 failed with 42P16 on a mid-list insert) and could NOT be dropped, because v_pnl_by_period depends on v_pnl_monthly and 0112 does not recreate it. 0113 follows the same append-only discipline even though nothing depends on it, because 'nothing depends on it today' is a fact with a shelf life. Every view restates security_invoker + revoke anon + grant authenticated after the create - create or replace does not preserve reloptions.",
    "DO NOT SEED A COLUMN WHOSE NULL CARRIES MEANING. A seed migration was drafted and deleted before it ran: it would have turned Umm Al Hamam's deliberate 'no potable' into 'potable at 0', undoing a real business decision under the guise of a backfill.",
    "NEVER RE-DERIVE A BUCKET LIST AT A CALL SITE. The Cost Mix doughnut rendered four hardcoded slices while operating_cost_sar had included filling since 0112 - short by 210 / 1,285 / 4,390 in Jun/Jul/Aug, unnoticed for three months. The direct-cost line needed no change: it reads v_daily_operations.direct_cost_sar as-is.",
    "AN EXPORTED 'use server' FUNCTION IS A LIVE ENDPOINT whether or not a component calls it - 'unused' is not 'unreachable'. updateTrip had no callers and wrote water_station/water_type with neither the gate nor the re-snapshot, so a station change through it would have stranded a frozen cost - a failure 0114 deliberately does NOT catch, because the trigger writes no money.",
    "ONE COLOUR PER COST BUCKET, in lib/cost-colors.ts. Reports and the Dashboard each hardcoded their own hexes and Payroll/Outsourced were SWAPPED between them - worse than two arbitrary palettes, because it looks consistent while meaning opposite things. A leaf module, since the two libs are siblings and neither is the other's parent.",
    "noUnusedLocals + noUnusedParameters are ON as of 6506f2e. A parameter genuinely required by a signature, interface or callback shape gets an _ prefix; it is never deleted, because that changes the shape the caller depends on."
  ],
  "uncommitted_files": [],
  "next_action": "Water Station Cost is complete AND browser-verified by Turki - the full checklist passed, including the re-test of the station-change re-snapshot after c76e731. Nothing is in flight. Open items are deferred rather than blocked - see CLAUDE.md section 7's 'Deferred - Water Station Cost' bullet.",
  "context_notes": "READ CLAUDE.md SECTION 7 FIRST - it is the durable record and this file is only a pointer to it; the Water Station Cost entry (migrations 0110-0114) sits just before the deferred tail. Working tree clean, 0 ahead / 0 behind origin/main. Highest migration on disk is 0114. FIVE THINGS WORTH CARRYING FORWARD. (1) THE BOTH-ENDS FREEZE RULE IS IN THE DECISIONS ABOVE AND IS THE ONE TO GET RIGHT. The first phrasing of this entry - 'a DELIVERED trip is never re-snapshotted' - is literally the sentence that produced the bug: it reads as 'target is delivered, leave it alone'. A trip is closed only when delivered BEFORE and AFTER the write. (2) HOW THAT BUG WAS CAUGHT MATTERS. Every reconciliation held - the statement faithfully reported the stored snapshot, so nothing summed wrong. Turki spotted 'Shas Water Station - 1 fill - 15 SAR' because he knew Shas charges 80. Sums agreeing does not mean the snapshots are right, and 0114 cannot help: the trigger refuses impossible station/type pairs but writes no money, and Shas does fill potable, so that move was legitimate. (3) BLAST RADIUS WAS MEASURED, NOT ASSUMED. A detector compared every frozen cost against its own station's prices; 356 mismatches were legitimate price edits on the same station and type, and exactly ONE was a stranded snapshot. Re-run that detector rather than eyeballing after any future change to the re-snapshot path. (4) TWO STANDING TEST SUITES need no diagnostic route and no auth bypass - tests/trip-station-gate.spec.ts (10, including two regressions pinning becoming-delivered and leaving-delivered) and tests/cost-colors.spec.ts (3). They drive pure functions, so unlike every earlier suite they do not rot at teardown. But note the bug they did NOT prevent lived in the WIRING - which stage setTripStage passed - not in the pure decision, so a real browser move is still the only proof of that layer. One test-authoring trap: an annotated const still narrows to its literal type, so mirroring the rule with `const from: TripStage = 'in_transit'` makes tsc reject the comparison (TS2367) and would have left a hardcoded boolean under test; use a function, where parameters do not narrow. (5) NEVER DELETE .next UNDER A RUNNING DEV SERVER. An entire page rendering unstyled was misdiagnosed as a UI regression; the cause was rm -rf .next while the dev server was attached. To verify a production build without stopping it, point distDir elsewhere - and note next build REWRITES tsconfig.json, so revert it afterwards."
}
